[build-system]
requires = ["setuptools>=68", "wheel"]
build-backend = "setuptools.build_meta"

[project]
name = "foundation-model-packages"
version = "0.0.1"
requires-python = ">=3.10"
dependencies = []

[tool.setuptools.packages.find]
where = ["."]
